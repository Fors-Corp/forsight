node_modules
dist
storybook-static
*.tsbuildinfo
.DS_Store
coverage

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# CodeGraph's local index — regenerable, not committed (run `codegraph init`).
/.codegraph/

# `forsight run --store badger` without --data-dir writes here when run from forsight/;
# an operator's data never belongs in the tree.
forsight/forsight-data/
